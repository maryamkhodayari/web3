{
  "name": "lproject",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server -w db.json --port 3000"
  },
  "devDependencies": {
    "vite": "^5.1.6"
  },
  "dependencies": {
    "json-server": "^1.0.0-alpha.23"
  }
}
